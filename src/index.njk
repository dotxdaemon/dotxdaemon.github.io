<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cinematography Gallery</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    .gallery {
      column-count: 3;
      column-gap: 1rem;
      max-width: 1200px;
      margin: auto;
      padding: 1rem;
    }
    .gallery img {
      width: 100%;
      margin-bottom: 1rem;
      break-inside: avoid;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <h1 style="text-align:center;">Cinematography Shots</h1>
  <div class="gallery">
    {% for image in collections.images %}
      <img src="{{ image.url }}" alt="{{ image.data.alt }}" loading="lazy">
    {% endfor %}
  </div>
</body>
</html>
